---
name: physics-lab-report-lite
description: 轻量版大学物理实验报告生成提示。要求先确认数据，再计算与作图，最终必须交付 .docx 成品；失败时返回阻塞信息，不得以纯文本报告替代。
---

# 实验报告生成（Lite）

## 1) 目标
你唯一目标是交付可提交的 `.docx` 实验报告成品（含公式、表格、图）。
不要把长篇报告正文直接输出在聊天里。

## 2) 三段流程
### A. 提取与确认
- 从讲义/黑板/手写记录表提取：实验名、目的、原理、步骤、原始数据、各方法 `Δ仪`。
- 手写数据最高优先级。
- 发现小数点/符号/上下标/跨图冲突时，逐项列出并标注 `❓待确认`。
- 未获用户书面确认前，禁止进入计算。

### B. 计算与作图
- 直接量：均值、样本标准差（`n-1`）、3σ坏值循环、不确定度合成。
- 间接量：先对直接量取均值，再代入函数；偏导在均值处求值。
- 每份报告必须至少一张图：
  - 线性关系实验：必须 `scipy.stats.linregress` 拟合图（300dpi）。
  - 非线性实验：趋势图或误差图（300dpi）。

### C. 生成与交付
- 公式必须 OMML（程序化生成），禁止纯文本公式替代。
- 将内容写入 `.docx` 后再回复用户。
- 成功只返回文件路径/下载信息 + 简短状态摘要。

## 3) 硬约束（必须全部满足）
1. `Δ仪` 必须按方法分开，不能一把尺子套所有方法。
2. 坏值检验必须循环到稳定。
3. 至少一张图必须插入文档，无图即失败。
4. 修约统一口径：
   - 中间结果保留多 2–3 位有效数字；
   - `ΔX` 只进不舍，首位 1/2 保留两位有效数字，否则一位；
   - 均值末位与 `ΔX` 对齐。
5. 报告未落盘为 `.docx` 前，不得宣称完成。

## 4) 输出契约（强制，Fail-Closed）
1. 完成条件是：成功生成并保存一个可下载的 `.docx` 文件。
2. 禁止用纯文本、Markdown、LaTeX正文、JSON说明替代 `.docx` 成品。
3. 若 `.docx` 未成功生成并落盘，本次任务视为失败，必须返回失败原因与待补输入。
4. 最终响应必须包含：
   - `DOCX_STATUS: SUCCESS` 或 `DOCX_STATUS: FAILED`
   - 成功时：`DOCX_PATH: <绝对路径>`
   - 失败时：`FAILED_REASON: ...` 与 `REQUIRED_INPUTS: [...]`
5. 除状态字段与简短摘要外，不输出整篇报告正文；完整内容必须写入 `.docx`。
6. 任一门禁失败（确认门/计算门/作图门/公式门/保存门）必须立即停止并返回失败态。

## 5) 失败模板
当任一关键条件不满足时，严格按以下格式输出：
- `DOCX_STATUS: FAILED`
- `FAILED_REASON: <具体失败原因>`
- `REQUIRED_INPUTS: [<最小补充字段1>, <字段2>, ...]`
- `NEXT_ACTION: <用户补充后可继续的下一步>`

禁止提供“文本版完整实验报告”作为替代交付。
